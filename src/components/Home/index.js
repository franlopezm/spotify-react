import React from 'react';


export default () => (
  <section className="home">
    <h2 className="home__title">
      Start <span className="home__title-one">searching</span> and get <span className="home__title-two">results</span> from the <span className="home__title-three">Spotify</span> <span className="home__title-four">API</span>.
    </h2>
  </section>
);
